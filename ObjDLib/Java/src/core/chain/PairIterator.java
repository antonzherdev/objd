package core.chain;

public class PairIterator<T> implements Iterator<T> {
    public Pair<T> pair;
    private int state;
    public boolean hasNext() {
    }
    public T next() {
    }
    public PairIterator(Pair<T> pair) {
    }
    static ClassType<PairIterator<T>> type;
}