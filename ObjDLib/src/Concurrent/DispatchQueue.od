package objd.concurrent

stub class DispatchQueue {
    static val default : DispatchQueue
    static val mainThread : DispatchQueue

    def async(f : () -> void)
}

@ObjC
stub class Thread {
    static def sleep(period : float)
}

@Java
@GenName("ThreadUtil")
stub class Thread {
    static def sleep(period : float)
}