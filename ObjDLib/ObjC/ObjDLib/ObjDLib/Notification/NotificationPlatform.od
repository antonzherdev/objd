package core.chain

stub class NotificationCenter {
    static val instance : NotificationCenter = NotificationCenter()

    def addObserver<S, D>(name : string, sender : S, block : (S, D) -> void) : NotificationObserver

    def post<S, D>(name : string, sender : S, data : D)
}

stub class NotificationObserver {
    def detach
}

